# Nature figure reference assets

本目录仅保存按需视觉参考。机器入口为 `assets/figure_assets.yaml`。

这些图片用于对照图型、留白、面板层级和信息密度，不是数据源、结果证据、固定配色模板或可直接复制的论文图。正式图仍必须满足：

```text
Python结果 → 标准工作簿 → Figure Contract → q{x}_plot.m → 论文图 → 正文结论
```

只有 `templates/figure/chart_selection.md` 判定视觉参考确有帮助时才加载相应资产；默认运行不读取全部图片。
