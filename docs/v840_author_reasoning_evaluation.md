# v8.4 建模求解叙事：实现、保全与试写记录

> Document role：historical implementation/evaluation record；runtime authority = none。  
> Evaluated release：v8.4.0。  
> Snapshot status：本文中的候选 head、当时 CI 与未完成验证只描述 v8.4 发布过程中的历史状态。  
> Current repository status：v8.4.0 已发布并已被后续 release 取代；当前规则以 active Authority 为准。

日期：2026-09-04。本文是 PR #108 的非运行时验收记录，不是新的写作 Authority，也不是论文作者身份或 AI 检测报告。

## 范围与基线

- 已发布基线：main v8.3.0，`123af04e9469d815ef03af19dcceab672d85b320`。
- 直接试写基线：PR #108 尚未发布的 v8.3.1，`1895fb80422ee7f9ca3254f0d07c2bc85a19dfe7`。
- 候选：同一 PR 中的 v8.4.0 增量；确切候选及 CI head 以 PR 提交记录为准。
- 输入先于候选规则修改固定：[四个合成案例](../tests/fixtures/writing_reasoning_cases.yaml)。输入提供完整事实而不是期望句式或标准答案；不使用用户比赛结果。
- 当前主助手按两个版本分别手工完成模型—求解—结果小节及清理复核。完整原稿、清理记录和局部清理压力输入保存在任务隔离输出目录 `outputs/v840-writing-evaluation/` 的 `baseline.md`、`candidate.md`，未写入用户论文或工作簿。该目录不属于安装包，也不是本文相对路径下的仓库目录。
- 观察维度在候选修改前固定：选择理由、数学连续性、求解细节、结果解释、自然表达、事实保全、详略、清理稳定性。

这是一次单助手、非盲、各案例各版本一次的局部试写。没有独立评审、多次生成或统计显著性检验；不能由此保证所有模型、所有题型或整篇论文都有相同收益。旧稿原本质量较好，不把旧稿标成失败以夸大改进。

## 实际变更落点

| 权威/消费者 | 本次变化 | 刻意不变 |
|---|---|---|
| Protocol §1、5 | 从当前事实恢复理由；必要解释不因没有新公式/数字被删 | 不增加思考日志，不把局部润色扩大成全文重写 |
| Protocol §7、7.1、7.2 | 指标、非显然变量、核心约束、假设近似、非优化关系、命题用途 | 原模型建立详细要求、公式链和核心模型汇总 |
| Protocol §7.3 | 作者声音为正常选择；示例按需移至一个参考页 | 不规定代词频次，不编造共识/经历，不把文风当来源证据 |
| Protocol §8 | 结构决定计算方法；关键实现动作的本题理由 | 编码、参数、停止、输出、完整证明与算法格式 |
| Protocol §9、11、13、16 | 事实/解释/猜想区分，发问有实际去向，图像直觉回到证据 | 不补造检验，不降低数值精度、引用或结论边界 |
| Reasoning | 只调整 paragraph_necessity.rule 与 prose_style.target | 其他完整语义字段不变，schema 仍 1.6.0 |
| Runtime | 三个已有阶段补充相关读取；一个条件示例分支 | 原 11 个阶段、顺序、gate、项目输入、能力声明不变 |
| Cleanup、Review | 按现有 Authority 保留依据、条件、数学作用和结果解释 | 不建立第二套细则、评分器、记录表或 Hard Fail |

原 §7.3 两个示例迁至一个可选参考页并扩充边界示例；不是删除章节能力。§7—9 原详细条目未缩减。既有前置章节、摘要等无需重复增写作者声音规则。

## 完整小节对照观察

### A：直接计算——不增加篇幅

两份水绝热混合，冷水 2 kg/20°C、热水 80°C、目标 40°C。两版均从等比热、无散热前提写热量守恒，解得 1 kg，并代回检验；候选正文与基线完全相同。没有强插“我们希望知道”或新增迭代算法、小节。这是无须改写的通过案例，不是性能提升。

### B：整数供给——把约束与结果的理由写出来

基线已定义整数批数、逐期约束、成本，枚举全部 9 个候选并用 `ceil(4/3)=2` 独立核对。候选新增的是明确的解释而非新模型：

> 供给以每批 3 单位交付，因此我们以批数 x_1、x_2 为决策，分别取整数 0、1、2；若允许任意连续供给量，就会纳入不符合整批规则的方案。

> 由于不能跨期储存或调剂，各期缺口必须由当期供给弥补，故应分别要求 3x_1≥4、3x_2≥4。仅约束总供给会丢失这一局部要求。又因允许超额供给，需求应写成下界而非等式。

候选同时解释每期余量 2 来自整批限制，最优性来自唯一可行解，而非很多可行方案之间的成本优势。两版模型、完整枚举、停止条件、(2,2)、供给(6,6)、成本10和检验边界一致。新增文字有可恢复的误解排除功能；代价是局部变长，只在该选择确有说明价值时使用。

### C：预测——均值与局部误差各说明什么

两版均保持训练拟合 `y_hat=2t+1`，留出预测(7,9,11)、观测(7,12,11)、残差(0,3,0)、MAE=1、最大误差3。基线已说明训练准确不意味着留出准确；候选进一步说明：

> 因此，我们认为评价这组预测时应同时保留平均误差和局部最大误差；平均误差为 1 不能理解成每一点都只偏离 1。

这里“我们认为”是有数据支持的评价取舍；不用生硬改成无主语，也不能据此推断原因。候选没有变更模型或新增鲁棒性结果。它补强的是平均指标与集中误差的关系，不是证明预测模型普遍有效。

### D：命题与算法——证明后具体能做什么

两版证明本体及伪代码完全相同：在 [0,2] 上用连续性、端点值和正导数证明唯一解，按指定二分算法计算。候选新增说明：`f(m)<3` 时严格递增排除所有 `x≤m`，反之保留左半区间；区间半宽控制位置误差，不能直接以函数残差替代。首次中点1精确命中、残差0、epsilon=1e-6、30次上限及未达精度失败状态均保留。

本例也可解析求解，但输入已指定展示二分，试写未擅自更换算法，也未虚构直接法的困难。命题前后更清楚，证明/伪代码内部没有变成聊天。该 Markdown 试写不是 LaTeX 排版质量证明。

## 清理保全与边界试验

四个完整候选小节在语义复核后不需要删除实质内容，清理稿因此与各自原稿相同；“不改”也是合法结果，不人为制造差稿来显示清理收益。

额外构造 B 的过度口语局部压力输入，明确它不是自然生成效果样本：

> 总量够了是不是就行了呢？我们觉得还是得看看每一期，毕竟跨期不能搬来搬去。说白了就是不能只看总量。我们需要分别要求 3x_1≥4、3x_2≥4；允许超额供给，所以这里不能写等式。

实际清理后：

> 总量充足是否足以满足两期需求？由于不能跨期储存或调剂，我们需要分别要求 3x_1≥4、3x_2≥4；允许超额供给，因此需求约束为下界而非等式。

删除重复与闲聊词，保留问题、作者声音、约束必要性、两个不等式和下界理由。以下边界也逐项作语义复核，并由既有 v8.3.1 测试检查可机检的部分；人工判断不冒充脚本结论：

| 场景 | 本次核验结论 |
|---|---|
| 同一枚举解释分别以“我们需要/需要/当前任务需要”表达 | 既有审计行为应相同，不以主语定优劣 |
| “我们采用遗传算法，该算法收敛速度快” | 仍缺具体结构依据，不能因有第一人称放行 |
| 没有新公式的约束必要性说明 | 保留无法由相邻内容替代的理由，不按新公式数量删除 |
| 用“我们认为全局最优”描述启发式结果 | 不放宽原有主张冲突检查 |
| “我们一致认为”但没有共识事实 | 改为当前有依据的选择，不补造团队讨论 |
| “我们希望知道误差在哪里”且已给出留出误差 | 回答 t=4 的误差3，不只写以后研究 |
| 由图提出成因想法但没有原因变量 | 保留观察和待检验方向，不写成因果结论 |
| 已有紧凑正确计算或证明/伪代码 | 不为自然口吻增加章节、代词、算法或删除证明步骤 |

## 确定性保全与工程验收

`tests/test_v840_author_reasoning_writing.py` 检查四案事实的数学自洽，以及从基线提取的以下快照；它不生成论文，不给文风或数学正确性打分：

- 9 个未改动章节（包括前置章节、摘要、跨问关系、跨文件承接）的原文快照。
- 模板入口/Manifest、LaTeX Adapter、命题和算法 Pack 的 Git blob 快照。
- 完整 Writing Reasoning 去除上述两个获准修改的文字字段后的语义快照。
- 完整 Runtime 去除版本、明确新增的读取和一个可选示例分支后的语义快照，保护原阶段、gate、schema、项目字段和加载边界。

v8.3 的四个写作文件整体快照随本次授权修改更新，其他保护快照保持原值；新增的局部与整体语义保全测试防止把扩大修改藏在哈希刷新中。保留 v7.18/v7.19、v7.20/v8.0.1 章节能力、命题/算法和数值相关既有测试；不删测试、不增加失败豁免。

本轮本地实测（Windows / Python 3.12.14，UTF-8）：

- `scripts/lint_skill.py`、`scripts/generate_indexes.py --check`、`git diff --check` 通过。
- `test_v8*.py` 144 项、`test_v718*.py` 12 项、`test_v719*.py` 11 项全部通过；包含新增 8 项保全/固定事实回归及已有 6 项作者声音审计行为回归。
- 全量测试执行 671 项，11 个错误均为清理临时 `.xlsx` 时的 `PermissionError: [WinError 32]`，无其他错误类别；不是全量通过。
- 同环境 main 基线执行 657 项，12 个同类错误；候选中有一项未在这次基线全量运行中出错，随后单独复跑 main 的 `test_user_execution_contract.py`，同一项也复现文件占用错误。因此候选 11 个出错测试均在基线复现；出现项及数量受运行组合影响，不能把数量减少说成修复。
- `skill-creator/scripts/quick_validate.py` 不接受仓库原有 `summary/triggers/version` 扩展 frontmatter。没有为迎合通用校验器改动仓库入口协议；以本仓库 lint 和入口一致性回归检验其约定。该通用检查未通过。
- 对基线逐行比较确认：Protocol 被替换的旧非空行仅限版本标题和 §7.3 的重组；§7.1/7.2、§8/9 原详细要求未删除。受保护 `templates/`、`packs/`、`scripts/` 及模型/数值/工作簿/项目状态契约相对 main 无差异。

当前 head 的 GitHub 全量矩阵与 LaTeX 编译结果须在推送后核验，状态以 PR #108 的同 SHA 检查记录为准，不沿用旧 head 的绿色状态。完整试写在自然表达和解释方面观察到上述局部改善，但尚无独立盲评或多次生成证据；不宣称“保证所有正文都有人工作者味道”或“通过 AI 检测”。

## 兼容与回滚

不新增项目字段、流程阶段、交付 gate、工作簿或模型记录，不迁移或自动改写旧论文；v8 原有兼容窗口不变。局部改写不能修改已确定模型、算法实现、参数或结果。未合并时可撤回本 PR 的 v8.4 增量；合并后按审查流程回退对应提交，仍保留用户论文。合并由用户另行明确指示，不由通过测试自动触发。
