# 代码精简检查

允许删除：无用 import、调试输出、进度条、重复逻辑、一次性包装、伪灵活配置、无必要类与未来脚手架。

不得删除：路径配置、随机种子、字段/空值/维度/单位检查、异常处理、目标函数、约束函数、约束违反检查、求解器状态、多算法/多初值验证、两类标准 Excel 工作簿输出和 `main()`。

Python 正式求解脚本中出现 `matplotlib`、`seaborn`、`savefig`、`plt.show` 或创建 `figures/`，默认判定为职责越界；除非用户明确要求临时调试图。
